% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_claude.R
\name{theme_claude}
\alias{theme_claude}
\title{Anthropic and Claude inspired ggplot2 theme}
\usage{
theme_claude(
  base_size = 12,
  font_title = "Poppins",
  font_text = "Poppins",
  font_subtitle = "Lora",
  grid = c("y", "x", "xy", "none"),
  axis_lines = TRUE,
  background = "white",
  ...
)
}
\arguments{
\item{base_size}{Base font size in points.}

\item{font_title}{Family for the plot title. Defaults to Poppins (with a
\code{"sans"} fallback).}

\item{font_text}{Family for axis, legend, and other text. Defaults to
Poppins (with a \code{"sans"} fallback).}

\item{font_subtitle}{Family for the subtitle and caption. Defaults to Lora
(with a \code{"serif"} fallback).}

\item{grid}{Which major grid lines to draw: \code{"y"} (default), \code{"x"}, \code{"xy"},
or \code{"none"}.}

\item{axis_lines}{Logical; draw strong axis lines and ticks on the left and
bottom? Defaults to \code{TRUE}.}

\item{background}{Panel and plot background fill. Use \code{"white"} (default) or
\code{"cloud"} for Anthropic's warm off-white, or any color string.}

\item{...}{Passed to \code{\link[ggplot2:ggtheme]{ggplot2::theme_minimal()}}.}
}
\value{
A ggplot2 theme object.
}
\description{
A clean, publication-ready theme modelled on the data visualizations in
Anthropic's model reports: a light background, light horizontal grid lines,
strong black axis lines, a bold geometric-sans title (Poppins), and a serif
subtitle (Lora). Custom fonts render on \code{ragg}/\code{svglite} devices; when the
bundled fonts are unavailable the theme falls back to generic families.
}
\details{
The bundled Poppins and Lora fonts only render on \code{ragg} or \code{svglite}
devices. On the base PDF/PostScript device (used by \verb{R CMD check}) custom
fonts are unavailable, so the examples below pass generic families. In your
own work, omit the \verb{font_*} arguments to use the Anthropic typefaces and
save with a \code{ragg} device, e.g.
\code{ggsave("plot.png", device = ragg::agg_png)}.
}
\examples{
library(ggplot2)

# Generic fonts keep the example device-agnostic.
ggplot(mtcars, aes(wt, mpg, color = factor(cyl))) +
  geom_point(size = 3) +
  labs(
    title = "Fuel efficiency by weight",
    subtitle = "Heavier cars travel fewer miles per gallon",
    color = "Cylinders"
  ) +
  scale_color_claude_d() +
  theme_claude(font_title = "sans", font_text = "sans", font_subtitle = "serif")

\dontrun{
# With the bundled Anthropic fonts (requires systemfonts + a ragg device):
ggplot(mtcars, aes(wt, mpg, color = factor(cyl))) +
  geom_point(size = 3) +
  theme_claude()
}
}
